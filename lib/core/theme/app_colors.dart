import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primaryColor = Color(0xFFFF9E00); // Naranja
  static const Color secondaryColor = Color(0xFFC4E2B4); // Verde claro
  static const Color accentColor = Color(0xFF2196F3); // Azul

  // Colores adicionales para la UI
  static const Color backgroundColor = Colors.white;
  static const Color textColor = Color(0xFF333333);
  static const Color errorColor = Color(0xFFE53935); // Rojo
  static const Color successColor = Color(0xFF43A047); // Verde
  static const Color warningColor = Color(0xFFFFA726); // Naranja ámbar

  // Colores para el formulario
  static const Color inputBorderColor = Color(0xFFE0E0E0);
  static const Color inputFocusedBorderColor = primaryColor;
  static const Color buttonTextColor = Colors.white;
}
